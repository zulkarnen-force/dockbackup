This repository contains the docker image for backup a database with labels in container:

# Development Rules

When modifying code, follow these rules:

1. Use docker image and bash script best practice and clean.
2. Adjust the example docker-compose when have a adjust to use the docker image.
3. Use command 'docker compose' in developing 

## Coding Guidelines

- Prefer clear and maintainable code
- Use descriptive variable names
- Create e example
- Up to date README.md